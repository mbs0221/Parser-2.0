struct Student {
    string name;
    int age;
    double score;
}

function getName(self) {
    return self.name;
}

function getAge(self) {
    return self.age;
}

function getScore(self) {
    return self.score;
}

function setScore(self, newScore) {
    self.score = newScore;
    return self.score;
}

let student1 = Student("Alice", 20, 95.5);
let student2 = Student("Bob", 22, 88.0);

println(student1.getName());
println(student1.getAge());
println(student1.getScore());

println(student2.getName());
println(student2.getAge());
println(student2.getScore());

println(student1.setScore(98.0));
println(student1.getScore());
