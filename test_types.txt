let str = "Hello World";
let len = str.length();
println("String length: " + len);

let arr = [1, 2, 3, 4, 5];
let size = arr.size();
println("Array size: " + size);

let dict = Dict("name", "test", "value", 42);
let keys = dict.keys();
println("Dict keys: " + keys);
println("Dict: " + dict);
println("Size: " + dict.size());
println("Contains key 'name': " + dict.containsKey("name"));
println("Contains value 42: " + dict.containsValue(42));

println("String toUpper: " + str.toUpper());
println("String toLower: " + str.toLower());
println("String trim: " + str.trim());
println("String indexOf 'World': " + str.indexOf("World"));
println("String lastIndexOf 'l': " + str.lastIndexOf("l"));
