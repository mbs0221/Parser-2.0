# 测试运算符和标点符号的区分
let a = 10 + 5;  # 算术运算符
let b = a * 2;   # 乘法运算符
let c = a > b;   # 比较运算符
let d = true && false;  # 逻辑运算符

# 标点符号测试
let arr = [1, 2, 3];  # 方括号和逗号
let obj = {x: 10, y: 20};  # 大括号和冒号
let func = (a, b) => a + b;  # 括号和箭头
